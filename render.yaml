services:
  # === Backend (FastAPI + Python) ===
  - type: web
    name: movie-recommender-api
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn recommender_api:app --host 0.0.0.0 --port 10000"
    pythonVersion: 3.12.0
    plan: free

  # === Frontend (Static HTML/JS/CSS) ===
  - type: web
    name: movie-recommender-frontend
    env: static
    rootDir: frontend
    buildCommand: ""
    staticPublishPath: .
    envVars:
      - key: API_BASE_URL
        value: https://movie-recommender-backend-a9wo.onrender.com
